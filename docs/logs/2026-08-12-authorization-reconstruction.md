# 2026-08-12 授权重建流水线

## 输入剖面

- 候选调用：54；场景组：5；
- 来源：Agent 轨迹 49，部署验证 5；
- 原始工具：exec 26、process 8、read 7、shell 5、web_fetch 2、web_search 2、browser 2、memory_search 1、write 1；
- 规范化工具族：shell 39、filesystem 8、http 6、memory 1；
- 未知规范化：0。

## 实现

1. 新增授权草案与已复核授权 Schema；
2. 新增确定性工具/动作/资源/接收方规范化器；只解析字符串，不执行样例内容；
3. 新增双人独立复核、第三人裁决和严格冻结逻辑；
4. 外部风险盲标包仅接收已冻结场景，并校验任务哈希与 `case_ids`；
5. 从风险上下文移除原始 `source_labels`，避免标签泄漏；
6. 生成本地授权复核工作簿，包含说明、A/B 复核、裁决、54 条调用清单和枚举值；
7. Excel 六张工作表均完成渲染检查，公式错误扫描为 0。

## 本地验收

- 5/5 授权草案通过 Schema；
- 草案 SHA-256：`d8372378e1cc3573e66b187eba7fc024243f27093eb1d1e0b5afd59dd03daafa`；
- `ruff check .` 通过；
- `pytest` 23/23 通过；
- 未完成复核时，授权冻结器会拒绝继续；存在分歧且未裁决时同样拒绝。

## 下一质量门

需要两名不同身份的复核员完成 5 个场景组授权重建。冻结成功后再生成 54 条（扣除被排除场景）风险盲标上下文，不提前查看或利用来源攻击标签。
